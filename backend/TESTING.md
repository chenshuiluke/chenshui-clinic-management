
### Run tests once

```bash
./backend/test.sh
```

This will:
- Start the database and SQS services
- Run all tests once
- Clean up and exit

### Run tests in watch mode (auto-rerun on changes)

```bash
./backend/test-watch.sh
```
